import { defineMessages } from 'react-intl';

export const scope = 'app.components.MultiDatePicker';

export default defineMessages({
  hide: {
    id: `${scope}.hide`,
    defaultMessage: 'HIDE'
  }
});
