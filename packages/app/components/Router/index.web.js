import {
  BrowserRouter as Router,
  Link,
  Route,
  withRouter
} from "react-router-dom";
import { createBrowserHistory } from "history";
const history = createBrowserHistory();
export { Router, Route, Link, withRouter, history };
