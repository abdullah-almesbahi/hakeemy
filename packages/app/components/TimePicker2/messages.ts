/*
 * TimePicker2 Messages
 *
 * This contains all the text for the TimePicker2 component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TimePicker2';

export default defineMessages({
  time: {
    id: `${scope}.time`,
    defaultMessage: 'Time'
  },
  pickTime: {
    id: `${scope}.pickTime`,
    defaultMessage: 'Pick a time'
  }
});
