/*
 * NoRecords Messages
 *
 * This contains all the text for the NoRecords component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.NoRecords";

export default defineMessages({
  noRecords: {
    id: `${scope}.noRecords`,
    defaultMessage: "No records found"
  }
});
