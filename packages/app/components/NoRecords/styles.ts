import { StyleSheet } from "react-native";
export default StyleSheet.create({
  noRecordContainder: {
    // borderWidth: 1,
    // borderColor: "red",
    flex: 1,
    alignItems: "center",
    justifyContent: "center"
  }
});
