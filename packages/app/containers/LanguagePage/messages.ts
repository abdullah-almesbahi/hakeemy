/*
 * LanguagePage Messages
 *
 * This contains all the text for the LanguagePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LanguagePage';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'CHANGE LANGUAGE'
  }
});
